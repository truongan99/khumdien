<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coin" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coin" >
        <activity android:name=".Controller.SelectWalletAvtActivity" >
        </activity>
        <activity android:name=".Controller.First_add_wallet" >

        </activity>
        <activity android:name=".Controller.LoginForm" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.Login" />
        <activity android:name=".Controller.AccountHome" />
        <activity android:name=".Controller.Home" />
        <activity android:name=".Controller.Signup" />
        <activity android:name=".Controller.MainActivity" >

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>