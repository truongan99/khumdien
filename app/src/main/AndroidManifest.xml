<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coin">
        <activity android:name=".Activity.SelectGroupDetail">
        </activity>
        <activity android:name=".Activity.AddGroup">

        </activity>
        <activity android:name=".Activity.SelectGroup" />
        <activity android:name=".Activity.AddTransaction" />
        <activity android:name=".Activity.SelectWalletAvtActivity" />
        <activity android:name=".Activity.First_add_wallet" />
        <activity android:name=".Activity.LoginForm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.Login" />
        <activity android:name=".Activity.AccountHome" />
        <activity android:name=".Activity.Signup" />
        <activity android:name=".Activity.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>